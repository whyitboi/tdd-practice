export function cesarCipher(someString, shiftFactor) {
  const alphabet = [..."abcdefghijklmnopqrstuvwxyz"];
  let result;
  for (let i = 0; i <= someString.length; i++) {
    alphabet.findIndex((letter) => {
      if (letter === someString[0]) {
        let currenPos = someString[0];
      }
    });
  }

  return result;
}
